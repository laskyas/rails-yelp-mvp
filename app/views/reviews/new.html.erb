 <div class="container">

  <%= simple_form_for([@restaurant, @review], url: restaurant_reviews_path) do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit class: 'btn btn-primary' %>
  <% end %>

</div>
